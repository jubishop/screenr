// Ubuntu 24.04 supplies Playwright dependencies, ShellCheck, and Caddy.
// Ignore unrelated runner sources without changing Ubuntu's mirrors or Signed-By.
Dir::Etc::sourcelist "sources.list.d/ubuntu.sources";
Dir::Etc::sourceparts "-";

// Required source failures must stop installation, including transient errors.
APT::Update::Error-Mode "any";
